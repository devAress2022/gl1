trigger ContentDocumentTest on ContentDocument (after insert) {
   /* system.debug('ContentDocumentTest');
    ContentDocument cd=new ContentDocument();
        for(ContentDocument c: trigger.new)
        {
            
            cd.Title= string.valueOf(System.now());
            cd.Id=c.Id;
            

            
        }
  update cd;*/
      
    

}