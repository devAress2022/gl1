trigger oppoName on Opportunity (before update) {
  /* Opportunity o =new Opportunity();
    for(Opportunity opp :trigger.new){
        List<OpportunityContactRole> opprole =[SELECT Id, OpportunityId, Role FROM OpportunityContactRole WHERE OpportunityId=:opp.Id ];
        if(opp.StageName  = 'Id. Decision Makers' ||  opp.StageName  = 'Perception Analysis'|| opp.StageName  = 'Proposal/Price Quote' || opp.StageName  = 'Negotiation/Review' || opp.StageName  = 'Closed Won' || opp.StageName  = 'Closed Lost' && opprole.size()<=0)
        {
               
           }
     
    }*/
   
}